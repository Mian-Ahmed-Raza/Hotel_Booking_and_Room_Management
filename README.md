# Hotel Booking and Room Management System


Starter project for SCD assignment.


## Run
1. Create a MySQL database and set environment variables: DB_HOST, DB_USER, DB_PASS, DB_NAME
2. Install dependencies: `pip install -r requirements.txt`
3. Run: `python -m app.main`


## Tests
Run `pytest`.